ERROR: `!pip install` is disabled in Tillicum shared Jupyter kernels.

Please create your own conda env and register it following:
https://hyak.uw.edu/docs/ood/jupyter

Directly running `pip install` is also not recommended; please always install packages outside the notebook in the terminal.
